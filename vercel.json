{
    "builds": [
      {
        "src": "webpack.config.js", 
        "use": "@vercel/static-build",
        "config": { "maxLambdaSize": "15mb" }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/" }
    ]
  }
